{% extends 'base.html.twig' %}

{% block body %}
    <h1>Provider</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ provider.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ provider.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ provider.address }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ provider.price }}</td>
            </tr>
            <tr>
                <th>Moq</th>
                <td>{{ provider.moq }}</td>
            </tr>
            <tr>
                <th>Productioncapacity</th>
                <td>{{ provider.productionCapacity }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{% if provider.active %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('provider_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('provider_edit', { 'id': provider.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
