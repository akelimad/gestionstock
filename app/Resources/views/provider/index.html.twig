{% extends 'base.html.twig' %}

{% block body %}

<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="content">
                        <h4 class="title">La liste des fournisseurs <a href=" {{path('provider_new')}} " class="btn btn-primary pull-right"><i class="fa fa-plus"></i> Nouveau</a></h4>
                        <div class="toolbar">
                            <!--        Here you can write extra buttons/actions for the toolbar              -->
                        </div>
                        <div class="material-datatables">
                            <table id="datatables" class="table table-striped table-no-bordered table-hover" style="width:100%;cellspacing:0">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Name</th>
                                        <th>Adresse</th>
                                        <th>Prix</th>
                                        <th>MOQ</th>
                                        <th>Capacité de Production</th>
                                        <th>Status</th>
                                        <th class="disabled-sorting text-right">Actions</th>
                                    </tr>
                                </thead>
                                {% for p in providers %}
                                <tbody>
                                    <tr>
                                        <td>{{p.id}}</td>
                                        <td>{{p.name}}</td>
                                        <td>{{p.address}}</td>
                                        <td>{{p.price}}</td>
                                        <td>{{p.moq}}</td>
                                        <td>{{p.productionCapacity}}</td>
                                        <td>{{p.active == 1 ? 'Active' : 'Not active'}}</td>
                                        <td class="text-right">
                                            <form name="form" method="post" action="/gestionstock/web/app_dev.php/provider/{{p.id}}">
                                                <input type="hidden" name="_method" value="DELETE">
                                                <a href="{{ path('provider_edit', { 'id': p.id }) }}" class="btn btn-simple btn-warning btn-icon edit"><i class="ti-pencil-alt"></i></a>
                                                <button type="submit" value="Delete" class="btn btn-simple btn-danger btn-icon remove"><i class="ti-close"></i></button>
                                                <input type="hidden" id="form__token" name="form[_token]" value="fRUQCoqbfyvNdQe41FfT013oadUySPUUYL1uz4ySpMI">
                                            </form>
                                        </td>
                                    </tr>
                                </tbody>
                                {% endfor %}
                                <tfoot>
                                    <tr>
                                        <th>Id</th>
                                        <th>Name</th>
                                        <th>Adresse</th>
                                        <th>Prix</th>
                                        <th>MOQ</th>
                                        <th>Capacité de Production</th>
                                        <th>Status</th>
                                        <th class="text-right">Actions</th>
                                    </tr>
                                </tfoot>
                            </table>
                        </div>
                    </div>
                    <!-- end content-->
                </div>
                <!--  end card  -->
            </div>
            <!-- end col-md-12 -->
        </div>
    </div>
</div>

{% endblock %}